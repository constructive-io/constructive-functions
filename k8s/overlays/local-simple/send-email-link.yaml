apiVersion: apps/v1
kind: Deployment
metadata:
  name: send-email-link
  labels:
    app: send-email-link
spec:
  replicas: 1
  selector:
    matchLabels:
      app: send-email-link
  template:
    metadata:
      labels:
        app: send-email-link
    spec:
      containers:
        - name: send-email-link
          image: constructive-functions:local
          command: ["npx"]
          args: ["tsx", "--watch", "generated/send-email-link/index.ts"]
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: pg-credentials
            - secretRef:
                name: mailgun-credentials
          env:
            - name: PORT
              value: "8080"
            - name: NODE_ENV
              value: "development"
            - name: LOG_LEVEL
              value: "info"
            - name: GRAPHQL_URL
              value: "http://constructive-server-admin.default.svc.cluster.local:3000/graphql"
            - name: META_GRAPHQL_URL
              value: "http://constructive-server-admin.default.svc.cluster.local:3000/graphql"
            - name: GRAPHQL_API_NAME
              value: "private"
            - name: DEFAULT_DATABASE_ID
              value: "constructive"
            - name: MAILGUN_FROM
              value: "no-reply@mg.constructive.io"
            - name: MAILGUN_REPLY
              value: "support@mg.constructive.io"
            - name: SEND_EMAIL_LINK_DRY_RUN
              value: "true"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: send-email-link
spec:
  selector:
    app: send-email-link
  ports:
    - port: 80
      targetPort: 8080
