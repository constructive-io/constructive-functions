apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "CNPG RW",
          "Group": "Constructive",
          "Host": "postgres.default.svc.cluster.local",
          "Port": 5432,
          "MaintenanceDB": "launchql",
          "Username": "postgres",
          "Password": "***REMOVED***",
          "SSLMode": "prefer"
        }
      }
    }

