apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "CNPG RW",
          "Group": "Constructive",
          "Host": "postgres.default.svc.cluster.local",
          "Port": 5432,
          "MaintenanceDB": "constructive",
          "Username": "postgres",
          "Password": "postgres123!",
          "SSLMode": "prefer"
        }
      }
    }

