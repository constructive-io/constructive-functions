apiVersion: v1
kind: Secret
metadata:
  name: constructive-uploads
type: Opaque
stringData:
  # Local MinIO credentials (dev-only; safe defaults)
  AWS_ACCESS_KEY_ID: minioadmin
  AWS_SECRET_ACCESS_KEY: minioadmin

