apiVersion: apps/v1
kind: Deployment
metadata:
  name: constructive-server-admin
spec:
  template:
    spec:
      containers:
        - name: server
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
