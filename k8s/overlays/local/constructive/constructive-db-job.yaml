apiVersion: batch/v1
kind: Job
metadata:
  name: constructive-db
spec:
  template:
    spec:
      containers:
        - name: lql
          env:
            - name: CONSTRUCTIVE_DOMAIN
              value: "localhost"
