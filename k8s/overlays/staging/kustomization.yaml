apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ./namespace.yaml
  - ./cnpg-backup.yaml
  # Create postgres-backup-credentials out-of-band with a secret manager,
  # ExternalSecrets, SOPS, or kubectl before enabling CNPG backups.
  # TODO: add staging-specific ingress/issuer and dashboard config patches.

patches:
  - path: ./cnpg-cluster-scale.yaml
  - path: ./cnpg-pooler-scale.yaml
  - path: ./constructive/constructive-scale.yaml
