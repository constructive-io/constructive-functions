-- Verify: schemas/constructive_infra_public/tables/platform_secret_definitions/fixtures/seed_built_in_secrets
-- made with <3 @ constructive.io
-- AUTO-GENERATED by scripts/register-functions.ts

BEGIN;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'EMAIL_SEND_USE_SMTP' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'LOCAL_APP_PORT' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'MAILGUN_API_KEY' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'MAILGUN_DOMAIN' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'MAILGUN_FROM' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'MAILGUN_REPLY' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'SEND_EMAIL_DRY_RUN' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'SEND_VERIFICATION_LINK_DRY_RUN' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'SMTP_FROM' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'SMTP_HOST' AND is_built_in = true;

SELECT 1 FROM constructive_infra_public.platform_secret_definitions
WHERE name = 'SMTP_PORT' AND is_built_in = true;

ROLLBACK;
