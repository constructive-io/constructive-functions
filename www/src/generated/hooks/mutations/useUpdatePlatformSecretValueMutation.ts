/**
 * Stores configured values for platform secrets (plaintext for now).
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSecretValueKeys } from '../query-keys';
import { platformSecretValueMutationKeys } from '../mutation-keys';
import type {
  PlatformSecretValueSelect,
  PlatformSecretValueWithRelations,
  PlatformSecretValuePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformSecretValueSelect,
  PlatformSecretValueWithRelations,
  PlatformSecretValuePatch,
} from '../../orm/input-types';
/**
 * Stores configured values for platform secrets (plaintext for now).
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformSecretValueMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformSecretValuePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformSecretValueMutation<S extends PlatformSecretValueSelect>(
  params: {
    selection: {
      fields: S & PlatformSecretValueSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretValueSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformSecretValue: {
          platformSecretValue: InferSelectResult<PlatformSecretValueWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformSecretValuePatch: PlatformSecretValuePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformSecretValue: {
      platformSecretValue: InferSelectResult<PlatformSecretValueWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformSecretValuePatch: PlatformSecretValuePatch;
  }
>;
export function useUpdatePlatformSecretValueMutation(
  params: {
    selection: SelectionConfig<PlatformSecretValueSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformSecretValuePatch: PlatformSecretValuePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformSecretValueSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformSecretValueMutationKeys.all,
    mutationFn: ({
      id,
      platformSecretValuePatch,
    }: {
      id: string;
      platformSecretValuePatch: PlatformSecretValuePatch;
    }) =>
      getClient()
        .platformSecretValue.update({
          where: {
            id,
          },
          data: platformSecretValuePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformSecretValueKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformSecretValueKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
