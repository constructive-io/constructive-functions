/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { jobKeys } from '../query-keys';
import { jobMutationKeys } from '../mutation-keys';
import type { JobSelect, JobWithRelations, JobPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { JobSelect, JobWithRelations, JobPatch } from '../../orm/input-types';
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateJobMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', jobPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateJobMutation<S extends JobSelect>(
  params: {
    selection: {
      fields: S & JobSelect;
    } & HookStrictSelect<NoInfer<S>, JobSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateJob: {
          job: InferSelectResult<JobWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        jobPatch: JobPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateJob: {
      job: InferSelectResult<JobWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    jobPatch: JobPatch;
  }
>;
export function useUpdateJobMutation(
  params: {
    selection: SelectionConfig<JobSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        jobPatch: JobPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<JobSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: jobMutationKeys.all,
    mutationFn: ({ id, jobPatch }: { id: string; jobPatch: JobPatch }) =>
      getClient()
        .job.update({
          where: {
            id,
          },
          data: jobPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: jobKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: jobKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
