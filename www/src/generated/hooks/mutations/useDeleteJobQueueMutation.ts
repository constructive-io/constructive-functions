/**
 * Queue metadata: tracks job counts and locking state for each named queue
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { jobQueueKeys } from '../query-keys';
import { jobQueueMutationKeys } from '../mutation-keys';
import type { JobQueueSelect, JobQueueWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { JobQueueSelect, JobQueueWithRelations } from '../../orm/input-types';
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteJobQueueMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ queueName: 'value-to-delete' });
 * ```
 */
export function useDeleteJobQueueMutation<S extends JobQueueSelect>(
  params: {
    selection: {
      fields: S & JobQueueSelect;
    } & HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteJobQueue: {
          jobQueue: InferSelectResult<JobQueueWithRelations, S>;
        };
      },
      Error,
      {
        queueName: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteJobQueue: {
      jobQueue: InferSelectResult<JobQueueWithRelations, S>;
    };
  },
  Error,
  {
    queueName: string;
  }
>;
export function useDeleteJobQueueMutation(
  params: {
    selection: SelectionConfig<JobQueueSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        queueName: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<JobQueueSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: jobQueueMutationKeys.all,
    mutationFn: ({ queueName }: { queueName: string }) =>
      getClient()
        .jobQueue.delete({
          where: {
            queueName,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: jobQueueKeys.detail(variables.queueName),
      });
      queryClient.invalidateQueries({
        queryKey: jobQueueKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
