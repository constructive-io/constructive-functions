/**
 * Queue metadata: tracks job counts and locking state for each named queue
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { jobQueueKeys } from '../query-keys';
import { jobQueueMutationKeys } from '../mutation-keys';
import type { JobQueueSelect, JobQueueWithRelations, JobQueuePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { JobQueueSelect, JobQueueWithRelations, JobQueuePatch } from '../../orm/input-types';
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateJobQueueMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ queueName: 'value-here', jobQueuePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateJobQueueMutation<S extends JobQueueSelect>(
  params: {
    selection: {
      fields: S & JobQueueSelect;
    } & HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateJobQueue: {
          jobQueue: InferSelectResult<JobQueueWithRelations, S>;
        };
      },
      Error,
      {
        queueName: string;
        jobQueuePatch: JobQueuePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateJobQueue: {
      jobQueue: InferSelectResult<JobQueueWithRelations, S>;
    };
  },
  Error,
  {
    queueName: string;
    jobQueuePatch: JobQueuePatch;
  }
>;
export function useUpdateJobQueueMutation(
  params: {
    selection: SelectionConfig<JobQueueSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        queueName: string;
        jobQueuePatch: JobQueuePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<JobQueueSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: jobQueueMutationKeys.all,
    mutationFn: ({
      queueName,
      jobQueuePatch,
    }: {
      queueName: string;
      jobQueuePatch: JobQueuePatch;
    }) =>
      getClient()
        .jobQueue.update({
          where: {
            queueName,
          },
          data: jobQueuePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: jobQueueKeys.detail(variables.queueName),
      });
      queryClient.invalidateQueries({
        queryKey: jobQueueKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
