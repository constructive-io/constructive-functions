/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { scheduledJobKeys } from '../query-keys';
import type {
  ScheduledJobSelect,
  ScheduledJobWithRelations,
  ScheduledJobFilter,
  ScheduledJobOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  ScheduledJobSelect,
  ScheduledJobWithRelations,
  ScheduledJobFilter,
  ScheduledJobOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const scheduledJobsQueryKey = scheduledJobKeys.list;
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useScheduledJobsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useScheduledJobsQuery<
  S extends ScheduledJobSelect,
  TData = {
    scheduledJobs: ConnectionResult<InferSelectResult<ScheduledJobWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ScheduledJobFilter, ScheduledJobOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
  } & Omit<
    UseQueryOptions<
      {
        scheduledJobs: ConnectionResult<InferSelectResult<ScheduledJobWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useScheduledJobsQuery(
  params: {
    selection: ListSelectionConfig<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: scheduledJobKeys.list(args),
    queryFn: () => getClient().scheduledJob.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```ts
 * const data = await fetchScheduledJobsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchScheduledJobsQuery<S extends ScheduledJobSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, ScheduledJobFilter, ScheduledJobOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
}): Promise<{
  scheduledJobs: ConnectionResult<InferSelectResult<ScheduledJobWithRelations, S>>;
}>;
export async function fetchScheduledJobsQuery(params: {
  selection: ListSelectionConfig<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>;
}) {
  const args = buildListSelectionArgs<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>(
    params.selection
  );
  return getClient().scheduledJob.findMany(args).unwrap();
}
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```ts
 * await prefetchScheduledJobsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchScheduledJobsQuery<S extends ScheduledJobSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ScheduledJobFilter, ScheduledJobOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
  }
): Promise<void>;
export async function prefetchScheduledJobsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<ScheduledJobSelect, ScheduledJobFilter, ScheduledJobOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: scheduledJobKeys.list(args),
    queryFn: () => getClient().scheduledJob.findMany(args).unwrap(),
  });
}
