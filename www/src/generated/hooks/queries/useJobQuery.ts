/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { jobKeys } from '../query-keys';
import type { JobSelect, JobWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { JobSelect, JobWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const jobQueryKey = jobKeys.detail;
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useJobQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useJobQuery<
  S extends JobSelect,
  TData = {
    job: InferSelectResult<JobWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, JobSelect>;
  } & Omit<
    UseQueryOptions<
      {
        job: InferSelectResult<JobWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useJobQuery(
  params: {
    id: string;
    selection: SelectionConfig<JobSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<JobSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: jobKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .job.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```ts
 * const data = await fetchJobQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchJobQuery<S extends JobSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, JobSelect>;
}): Promise<{
  job: InferSelectResult<JobWithRelations, S> | null;
}>;
export async function fetchJobQuery(params: {
  id: string;
  selection: SelectionConfig<JobSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<JobSelect>(params.selection);
  return getClient()
    .job.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```ts
 * await prefetchJobQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchJobQuery<S extends JobSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, JobSelect>;
  }
): Promise<void>;
export async function prefetchJobQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<JobSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<JobSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: jobKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .job.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
