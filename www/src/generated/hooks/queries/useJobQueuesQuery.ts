/**
 * Queue metadata: tracks job counts and locking state for each named queue
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { jobQueueKeys } from '../query-keys';
import type {
  JobQueueSelect,
  JobQueueWithRelations,
  JobQueueFilter,
  JobQueueOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  JobQueueSelect,
  JobQueueWithRelations,
  JobQueueFilter,
  JobQueueOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const jobQueuesQueryKey = jobQueueKeys.list;
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useJobQueuesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useJobQueuesQuery<
  S extends JobQueueSelect,
  TData = {
    jobQueues: ConnectionResult<InferSelectResult<JobQueueWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, JobQueueFilter, JobQueueOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  } & Omit<
    UseQueryOptions<
      {
        jobQueues: ConnectionResult<InferSelectResult<JobQueueWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useJobQueuesQuery(
  params: {
    selection: ListSelectionConfig<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: jobQueueKeys.list(args),
    queryFn: () => getClient().jobQueue.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```ts
 * const data = await fetchJobQueuesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchJobQueuesQuery<S extends JobQueueSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, JobQueueFilter, JobQueueOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, JobQueueSelect>;
}): Promise<{
  jobQueues: ConnectionResult<InferSelectResult<JobQueueWithRelations, S>>;
}>;
export async function fetchJobQueuesQuery(params: {
  selection: ListSelectionConfig<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>;
}) {
  const args = buildListSelectionArgs<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>(
    params.selection
  );
  return getClient().jobQueue.findMany(args).unwrap();
}
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```ts
 * await prefetchJobQueuesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchJobQueuesQuery<S extends JobQueueSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, JobQueueFilter, JobQueueOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  }
): Promise<void>;
export async function prefetchJobQueuesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<JobQueueSelect, JobQueueFilter, JobQueueOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: jobQueueKeys.list(args),
    queryFn: () => getClient().jobQueue.findMany(args).unwrap(),
  });
}
