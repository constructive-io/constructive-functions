/**
 * Stores configured values for platform secrets (plaintext for now).
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSecretValueKeys } from '../query-keys';
import type {
  PlatformSecretValueSelect,
  PlatformSecretValueWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformSecretValueSelect,
  PlatformSecretValueWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformSecretValueQueryKey = platformSecretValueKeys.detail;
/**
 * Stores configured values for platform secrets (plaintext for now).
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformSecretValueQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformSecretValueQuery<
  S extends PlatformSecretValueSelect,
  TData = {
    platformSecretValue: InferSelectResult<PlatformSecretValueWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretValueSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformSecretValue: InferSelectResult<PlatformSecretValueWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformSecretValueQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformSecretValueSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformSecretValueSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformSecretValueKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSecretValue.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Stores configured values for platform secrets (plaintext for now).
 *
 * @example
 * ```ts
 * const data = await fetchPlatformSecretValueQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformSecretValueQuery<S extends PlatformSecretValueSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformSecretValueSelect>;
}): Promise<{
  platformSecretValue: InferSelectResult<PlatformSecretValueWithRelations, S> | null;
}>;
export async function fetchPlatformSecretValueQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformSecretValueSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformSecretValueSelect>(params.selection);
  return getClient()
    .platformSecretValue.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Stores configured values for platform secrets (plaintext for now).
 *
 * @example
 * ```ts
 * await prefetchPlatformSecretValueQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformSecretValueQuery<S extends PlatformSecretValueSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretValueSelect>;
  }
): Promise<void>;
export async function prefetchPlatformSecretValueQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformSecretValueSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformSecretValueSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformSecretValueKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSecretValue.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
