/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { scheduledJobKeys } from '../query-keys';
import type { ScheduledJobSelect, ScheduledJobWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ScheduledJobSelect, ScheduledJobWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const scheduledJobQueryKey = scheduledJobKeys.detail;
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useScheduledJobQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useScheduledJobQuery<
  S extends ScheduledJobSelect,
  TData = {
    scheduledJob: InferSelectResult<ScheduledJobWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
  } & Omit<
    UseQueryOptions<
      {
        scheduledJob: InferSelectResult<ScheduledJobWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useScheduledJobQuery(
  params: {
    id: string;
    selection: SelectionConfig<ScheduledJobSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ScheduledJobSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: scheduledJobKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .scheduledJob.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```ts
 * const data = await fetchScheduledJobQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchScheduledJobQuery<S extends ScheduledJobSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
}): Promise<{
  scheduledJob: InferSelectResult<ScheduledJobWithRelations, S> | null;
}>;
export async function fetchScheduledJobQuery(params: {
  id: string;
  selection: SelectionConfig<ScheduledJobSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ScheduledJobSelect>(params.selection);
  return getClient()
    .scheduledJob.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Recurring/cron-style job definitions: each row spawns jobs on a schedule, optionally scoped to a database
 *
 * @example
 * ```ts
 * await prefetchScheduledJobQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchScheduledJobQuery<S extends ScheduledJobSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ScheduledJobSelect>;
  }
): Promise<void>;
export async function prefetchScheduledJobQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ScheduledJobSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ScheduledJobSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: scheduledJobKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .scheduledJob.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
