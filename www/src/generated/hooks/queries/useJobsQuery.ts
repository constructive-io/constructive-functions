/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { jobKeys } from '../query-keys';
import type { JobSelect, JobWithRelations, JobFilter, JobOrderBy } from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type { JobSelect, JobWithRelations, JobFilter, JobOrderBy } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const jobsQueryKey = jobKeys.list;
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useJobsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useJobsQuery<
  S extends JobSelect,
  TData = {
    jobs: ConnectionResult<InferSelectResult<JobWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, JobFilter, JobOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, JobSelect>;
  } & Omit<
    UseQueryOptions<
      {
        jobs: ConnectionResult<InferSelectResult<JobWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useJobsQuery(
  params: {
    selection: ListSelectionConfig<JobSelect, JobFilter, JobOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<JobSelect, JobFilter, JobOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: jobKeys.list(args),
    queryFn: () => getClient().job.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```ts
 * const data = await fetchJobsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchJobsQuery<S extends JobSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, JobFilter, JobOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, JobSelect>;
}): Promise<{
  jobs: ConnectionResult<InferSelectResult<JobWithRelations, S>>;
}>;
export async function fetchJobsQuery(params: {
  selection: ListSelectionConfig<JobSelect, JobFilter, JobOrderBy>;
}) {
  const args = buildListSelectionArgs<JobSelect, JobFilter, JobOrderBy>(params.selection);
  return getClient().job.findMany(args).unwrap();
}
/**
 * Background job queue: each row is a pending or in-progress task, optionally scoped to a database
 *
 * @example
 * ```ts
 * await prefetchJobsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchJobsQuery<S extends JobSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, JobFilter, JobOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, JobSelect>;
  }
): Promise<void>;
export async function prefetchJobsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<JobSelect, JobFilter, JobOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<JobSelect, JobFilter, JobOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: jobKeys.list(args),
    queryFn: () => getClient().job.findMany(args).unwrap(),
  });
}
