/**
 * Queue metadata: tracks job counts and locking state for each named queue
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { jobQueueKeys } from '../query-keys';
import type { JobQueueSelect, JobQueueWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { JobQueueSelect, JobQueueWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const jobQueueQueryKey = jobQueueKeys.detail;
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useJobQueueQuery({
 *   queueName: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useJobQueueQuery<
  S extends JobQueueSelect,
  TData = {
    jobQueue: InferSelectResult<JobQueueWithRelations, S> | null;
  },
>(
  params: {
    queueName: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  } & Omit<
    UseQueryOptions<
      {
        jobQueue: InferSelectResult<JobQueueWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useJobQueueQuery(
  params: {
    queueName: string;
    selection: SelectionConfig<JobQueueSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<JobQueueSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: jobQueueKeys.detail(params.queueName),
    queryFn: () =>
      getClient()
        .jobQueue.findOne({
          queueName: params.queueName,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```ts
 * const data = await fetchJobQueueQuery({
 *   queueName: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchJobQueueQuery<S extends JobQueueSelect>(params: {
  queueName: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, JobQueueSelect>;
}): Promise<{
  jobQueue: InferSelectResult<JobQueueWithRelations, S> | null;
}>;
export async function fetchJobQueueQuery(params: {
  queueName: string;
  selection: SelectionConfig<JobQueueSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<JobQueueSelect>(params.selection);
  return getClient()
    .jobQueue.findOne({
      queueName: params.queueName,
      select: args.select,
    })
    .unwrap();
}
/**
 * Queue metadata: tracks job counts and locking state for each named queue
 *
 * @example
 * ```ts
 * await prefetchJobQueueQuery(queryClient, { queueName: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchJobQueueQuery<S extends JobQueueSelect>(
  queryClient: QueryClient,
  params: {
    queueName: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, JobQueueSelect>;
  }
): Promise<void>;
export async function prefetchJobQueueQuery(
  queryClient: QueryClient,
  params: {
    queueName: string;
    selection: SelectionConfig<JobQueueSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<JobQueueSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: jobQueueKeys.detail(params.queueName),
    queryFn: () =>
      getClient()
        .jobQueue.findOne({
          queueName: params.queueName,
          select: args.select,
        })
        .unwrap(),
  });
}
