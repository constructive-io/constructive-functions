/**
 * Registry of valid config keys — declares which config entries the platform recognizes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformConfigDefinitionKeys } from '../query-keys';
import { platformConfigDefinitionMutationKeys } from '../mutation-keys';
import type {
  PlatformConfigDefinitionSelect,
  PlatformConfigDefinitionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformConfigDefinitionSelect,
  PlatformConfigDefinitionWithRelations,
} from '../../orm/input-types';
/**
 * Registry of valid config keys — declares which config entries the platform recognizes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformConfigDefinitionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformConfigDefinitionMutation<S extends PlatformConfigDefinitionSelect>(
  params: {
    selection: {
      fields: S & PlatformConfigDefinitionSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformConfigDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformConfigDefinition: {
          platformConfigDefinition: InferSelectResult<PlatformConfigDefinitionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformConfigDefinition: {
      platformConfigDefinition: InferSelectResult<PlatformConfigDefinitionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformConfigDefinitionMutation(
  params: {
    selection: SelectionConfig<PlatformConfigDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformConfigDefinitionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformConfigDefinitionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformConfigDefinition.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformConfigDefinitionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformConfigDefinitionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
