/**
 * Create mutation hook for User
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type { CreateUserInput,UserSelect, UserWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { userMutationKeys } from '../mutation-keys';
import { userKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { CreateUserInput,UserSelect, UserWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for creating a User
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateUserMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateUserMutation<S extends UserSelect>(
  params: {
    selection: {
      fields: S & UserSelect;
    } & HookStrictSelect<NoInfer<S>, UserSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createUser: {
          user: InferSelectResult<UserWithRelations, S>;
        };
      },
      Error,
      CreateUserInput['user']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createUser: {
      user: InferSelectResult<UserWithRelations, S>;
    };
  },
  Error,
  CreateUserInput['user']
>;
export function useCreateUserMutation(
  params: {
    selection: SelectionConfig<UserSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateUserInput['user']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<UserSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: userMutationKeys.create(),
    mutationFn: (data: CreateUserInput['user']) =>
      getClient()
        .user.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: userKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
