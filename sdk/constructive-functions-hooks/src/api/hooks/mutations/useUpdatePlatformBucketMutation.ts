/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type {
  PlatformBucketPatch,
  PlatformBucketSelect,
  PlatformBucketWithRelations,
} from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { platformBucketMutationKeys } from '../mutation-keys';
import { platformBucketKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type {
  PlatformBucketPatch,
  PlatformBucketSelect,
  PlatformBucketWithRelations,
} from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformBucketPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformBucketMutation<S extends PlatformBucketSelect>(
  params: {
    selection: {
      fields: S & PlatformBucketSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformBucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformBucket: {
          platformBucket: InferSelectResult<PlatformBucketWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformBucketPatch: PlatformBucketPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformBucket: {
      platformBucket: InferSelectResult<PlatformBucketWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformBucketPatch: PlatformBucketPatch;
  }
>;
export function useUpdatePlatformBucketMutation(
  params: {
    selection: SelectionConfig<PlatformBucketSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformBucketPatch: PlatformBucketPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformBucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformBucketMutationKeys.all,
    mutationFn: ({
      id,
      platformBucketPatch,
    }: {
      id: string;
      platformBucketPatch: PlatformBucketPatch;
    }) =>
      getClient()
        .platformBucket.update({
          where: {
            id,
          },
          data: platformBucketPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformBucketKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformBucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
