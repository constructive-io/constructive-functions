/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBucketKeys } from '../query-keys';
import { platformBucketMutationKeys } from '../mutation-keys';
import type {
  PlatformBucketSelect,
  PlatformBucketWithRelations,
  CreatePlatformBucketInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformBucketSelect,
  PlatformBucketWithRelations,
  CreatePlatformBucketInput,
} from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlatformBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlatformBucketMutation<S extends PlatformBucketSelect>(
  params: {
    selection: {
      fields: S & PlatformBucketSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformBucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlatformBucket: {
          platformBucket: InferSelectResult<PlatformBucketWithRelations, S>;
        };
      },
      Error,
      CreatePlatformBucketInput['platformBucket']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlatformBucket: {
      platformBucket: InferSelectResult<PlatformBucketWithRelations, S>;
    };
  },
  Error,
  CreatePlatformBucketInput['platformBucket']
>;
export function useCreatePlatformBucketMutation(
  params: {
    selection: SelectionConfig<PlatformBucketSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreatePlatformBucketInput['platformBucket']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformBucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformBucketMutationKeys.create(),
    mutationFn: (data: CreatePlatformBucketInput['platformBucket']) =>
      getClient()
        .platformBucket.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: platformBucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
