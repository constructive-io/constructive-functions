/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type { PlatformFileSelect, PlatformFileWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { platformFileMutationKeys } from '../mutation-keys';
import { platformFileKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { PlatformFileSelect, PlatformFileWithRelations } from '../../orm/input-types';
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformFileMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformFileMutation<S extends PlatformFileSelect>(
  params: {
    selection: {
      fields: S & PlatformFileSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformFileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformFile: {
          platformFile: InferSelectResult<PlatformFileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformFile: {
      platformFile: InferSelectResult<PlatformFileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformFileMutation(
  params: {
    selection: SelectionConfig<PlatformFileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformFileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformFileMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformFile.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformFileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformFileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
