/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type {
  PlatformConfigPatch,
  PlatformConfigSelect,
  PlatformConfigWithRelations,
} from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { platformConfigMutationKeys } from '../mutation-keys';
import { platformConfigKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type {
  PlatformConfigPatch,
  PlatformConfigSelect,
  PlatformConfigWithRelations,
} from '../../orm/input-types';
/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformConfigPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformConfigMutation<S extends PlatformConfigSelect>(
  params: {
    selection: {
      fields: S & PlatformConfigSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformConfig: {
          platformConfig: InferSelectResult<PlatformConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformConfigPatch: PlatformConfigPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformConfig: {
      platformConfig: InferSelectResult<PlatformConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformConfigPatch: PlatformConfigPatch;
  }
>;
export function useUpdatePlatformConfigMutation(
  params: {
    selection: SelectionConfig<PlatformConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformConfigPatch: PlatformConfigPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformConfigMutationKeys.all,
    mutationFn: ({
      id,
      platformConfigPatch,
    }: {
      id: string;
      platformConfigPatch: PlatformConfigPatch;
    }) =>
      getClient()
        .platformConfig.update({
          where: {
            id,
          },
          data: platformConfigPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
