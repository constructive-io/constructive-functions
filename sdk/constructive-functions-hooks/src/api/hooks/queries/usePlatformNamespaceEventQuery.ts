/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformNamespaceEventKeys } from '../query-keys';
import type {
  PlatformNamespaceEventSelect,
  PlatformNamespaceEventWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformNamespaceEventSelect,
  PlatformNamespaceEventWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformNamespaceEventQueryKey = platformNamespaceEventKeys.detail;
/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformNamespaceEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformNamespaceEventQuery<
  S extends PlatformNamespaceEventSelect,
  TData = {
    platformNamespaceEvent: InferSelectResult<PlatformNamespaceEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformNamespaceEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformNamespaceEvent: InferSelectResult<PlatformNamespaceEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformNamespaceEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformNamespaceEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformNamespaceEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformNamespaceEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformNamespaceEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 *
 * @example
 * ```ts
 * const data = await fetchPlatformNamespaceEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformNamespaceEventQuery<
  S extends PlatformNamespaceEventSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformNamespaceEventSelect>;
}): Promise<{
  platformNamespaceEvent: InferSelectResult<PlatformNamespaceEventWithRelations, S> | null;
}>;
export async function fetchPlatformNamespaceEventQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformNamespaceEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformNamespaceEventSelect>(params.selection);
  return getClient()
    .platformNamespaceEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 *
 * @example
 * ```ts
 * await prefetchPlatformNamespaceEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformNamespaceEventQuery<S extends PlatformNamespaceEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformNamespaceEventSelect>;
  }
): Promise<void>;
export async function prefetchPlatformNamespaceEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformNamespaceEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformNamespaceEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformNamespaceEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformNamespaceEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
