/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { QueryClient,UseQueryOptions, UseQueryResult } from '@tanstack/react-query';
import { useQuery } from '@tanstack/react-query';

import type { PlatformConfigSelect, PlatformConfigWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { platformConfigKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { PlatformConfigSelect, PlatformConfigWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformConfigQueryKey = platformConfigKeys.detail;
/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformConfigQuery<
  S extends PlatformConfigSelect,
  TData = {
    platformConfig: InferSelectResult<PlatformConfigWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformConfig: InferSelectResult<PlatformConfigWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformConfigQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformConfigSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformConfigSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```ts
 * const data = await fetchPlatformConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformConfigQuery<S extends PlatformConfigSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformConfigSelect>;
}): Promise<{
  platformConfig: InferSelectResult<PlatformConfigWithRelations, S> | null;
}>;
export async function fetchPlatformConfigQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformConfigSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformConfigSelect>(params.selection);
  return getClient()
    .platformConfig.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * App-level plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```ts
 * await prefetchPlatformConfigQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformConfigQuery<S extends PlatformConfigSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformConfigSelect>;
  }
): Promise<void>;
export async function prefetchPlatformConfigQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformConfigSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformConfigSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
