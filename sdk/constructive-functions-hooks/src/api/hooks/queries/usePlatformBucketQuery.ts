/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { QueryClient,UseQueryOptions, UseQueryResult } from '@tanstack/react-query';
import { useQuery } from '@tanstack/react-query';

import type { PlatformBucketSelect, PlatformBucketWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { platformBucketKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { PlatformBucketSelect, PlatformBucketWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBucketQueryKey = platformBucketKeys.detail;
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformBucketQuery<
  S extends PlatformBucketSelect,
  TData = {
    platformBucket: InferSelectResult<PlatformBucketWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBucketSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBucket: InferSelectResult<PlatformBucketWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBucketQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformBucketSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformBucketSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformBucketQuery<S extends PlatformBucketSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformBucketSelect>;
}): Promise<{
  platformBucket: InferSelectResult<PlatformBucketWithRelations, S> | null;
}>;
export async function fetchPlatformBucketQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformBucketSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformBucketSelect>(params.selection);
  return getClient()
    .platformBucket.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * await prefetchPlatformBucketQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformBucketQuery<S extends PlatformBucketSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBucketSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBucketQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformBucketSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformBucketSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
