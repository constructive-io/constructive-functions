/**
 * GraphQL SDK
 * @generated by @constructive-io/graphql-codegen
 *
 * Tables: GetAllRecord, PlatformFunctionGraphRef, PlatformFunctionGraphStore, PlatformFunctionGraphObject, OrgFunctionExecutionLog, PlatformFunctionGraphExecutionOutput, PlatformFunctionGraphCommit, PlatformSecretDefinition, PlatformFunctionExecutionLog, PlatformFunctionGraphExecutionNodeState, PlatformFunctionGraph, PlatformComputeLog, PlatformUsageDaily, OrgFunctionInvocation, PlatformFunctionInvocation, PlatformFunctionGraphExecution, PlatformFunctionDefinition
 *
 * Usage:
 *
 * 1. Configure the client:
 * ```ts
 * import { configure } from './generated';
 *
 * configure({
 *   endpoint: 'https://api.example.com/graphql',
 *   headers: { Authorization: 'Bearer <token>' },
 * });
 * ```
 *
 * 2. Use the hooks:
 * ```tsx
 * import { useCarsQuery, useCreateCarMutation } from './generated';
 *
 * function MyComponent() {
 *   const { data, isLoading } = useCarsQuery({
 *     selection: { fields: { id: true }, first: 10 },
 *   });
 *   const { mutate } = useCreateCarMutation({
 *     selection: { fields: { id: true } },
 *   });
 *   // ...
 * }
 * ```
 */
export * from './client';
export * from './invalidation';
export * from './mutation-keys';
export * from './mutations';
export * from './queries';
export * from './query-keys';
