/**
 * Update mutation hook for PlatformUsageDaily
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformUsageDailyKeys } from '../query-keys';
import { platformUsageDailyMutationKeys } from '../mutation-keys';
import type {
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
  PlatformUsageDailyPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
  PlatformUsageDailyPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a PlatformUsageDaily
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformUsageDailyMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformUsageDailyPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformUsageDailyMutation<S extends PlatformUsageDailySelect>(
  params: {
    selection: {
      fields: S & PlatformUsageDailySelect;
    } & HookStrictSelect<NoInfer<S>, PlatformUsageDailySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformUsageDaily: {
          platformUsageDaily: InferSelectResult<PlatformUsageDailyWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformUsageDailyPatch: PlatformUsageDailyPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformUsageDaily: {
      platformUsageDaily: InferSelectResult<PlatformUsageDailyWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformUsageDailyPatch: PlatformUsageDailyPatch;
  }
>;
export function useUpdatePlatformUsageDailyMutation(
  params: {
    selection: SelectionConfig<PlatformUsageDailySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformUsageDailyPatch: PlatformUsageDailyPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformUsageDailySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformUsageDailyMutationKeys.all,
    mutationFn: ({
      id,
      platformUsageDailyPatch,
    }: {
      id: string;
      platformUsageDailyPatch: PlatformUsageDailyPatch;
    }) =>
      getClient()
        .platformUsageDaily.update({
          where: {
            id,
          },
          data: platformUsageDailyPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformUsageDailyKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformUsageDailyKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
