/**
 * Delete mutation hook for PlatformUsageDaily
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformUsageDailyKeys } from '../query-keys';
import { platformUsageDailyMutationKeys } from '../mutation-keys';
import type {
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
} from '../../orm/input-types';
/**
 * Mutation hook for deleting a PlatformUsageDaily with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformUsageDailyMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformUsageDailyMutation<S extends PlatformUsageDailySelect>(
  params: {
    selection: {
      fields: S & PlatformUsageDailySelect;
    } & HookStrictSelect<NoInfer<S>, PlatformUsageDailySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformUsageDaily: {
          platformUsageDaily: InferSelectResult<PlatformUsageDailyWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformUsageDaily: {
      platformUsageDaily: InferSelectResult<PlatformUsageDailyWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformUsageDailyMutation(
  params: {
    selection: SelectionConfig<PlatformUsageDailySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformUsageDailySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformUsageDailyMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformUsageDaily.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformUsageDailyKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformUsageDailyKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
