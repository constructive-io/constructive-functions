/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformFunctionGraphKeys } from '../query-keys';
import type {
  PlatformFunctionGraphSelect,
  PlatformFunctionGraphWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformFunctionGraphSelect,
  PlatformFunctionGraphWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformFunctionGraphQueryKey = platformFunctionGraphKeys.detail;
/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformFunctionGraphQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformFunctionGraphQuery<
  S extends PlatformFunctionGraphSelect,
  TData = {
    platformFunctionGraph: InferSelectResult<PlatformFunctionGraphWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformFunctionGraphSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformFunctionGraph: InferSelectResult<PlatformFunctionGraphWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformFunctionGraphQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformFunctionGraphSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformFunctionGraphSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformFunctionGraphKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformFunctionGraph.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 *
 * @example
 * ```ts
 * const data = await fetchPlatformFunctionGraphQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformFunctionGraphQuery<
  S extends PlatformFunctionGraphSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformFunctionGraphSelect>;
}): Promise<{
  platformFunctionGraph: InferSelectResult<PlatformFunctionGraphWithRelations, S> | null;
}>;
export async function fetchPlatformFunctionGraphQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformFunctionGraphSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformFunctionGraphSelect>(params.selection);
  return getClient()
    .platformFunctionGraph.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 *
 * @example
 * ```ts
 * await prefetchPlatformFunctionGraphQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformFunctionGraphQuery<S extends PlatformFunctionGraphSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformFunctionGraphSelect>;
  }
): Promise<void>;
export async function prefetchPlatformFunctionGraphQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformFunctionGraphSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformFunctionGraphSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformFunctionGraphKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformFunctionGraph.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
