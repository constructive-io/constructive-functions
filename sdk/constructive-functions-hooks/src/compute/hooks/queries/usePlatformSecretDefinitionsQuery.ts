/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformSecretDefinitionKeys } from '../query-keys';
import type {
  PlatformSecretDefinitionSelect,
  PlatformSecretDefinitionWithRelations,
  PlatformSecretDefinitionFilter,
  PlatformSecretDefinitionOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformSecretDefinitionSelect,
  PlatformSecretDefinitionWithRelations,
  PlatformSecretDefinitionFilter,
  PlatformSecretDefinitionOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformSecretDefinitionsQueryKey = platformSecretDefinitionKeys.list;
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformSecretDefinitionsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformSecretDefinitionsQuery<
  S extends PlatformSecretDefinitionSelect,
  TData = {
    platformSecretDefinitions: ConnectionResult<
      InferSelectResult<PlatformSecretDefinitionWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformSecretDefinitionFilter, PlatformSecretDefinitionOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformSecretDefinitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformSecretDefinitions: ConnectionResult<
          InferSelectResult<PlatformSecretDefinitionWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformSecretDefinitionsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformSecretDefinitionSelect,
      PlatformSecretDefinitionFilter,
      PlatformSecretDefinitionOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformSecretDefinitionSelect,
    PlatformSecretDefinitionFilter,
    PlatformSecretDefinitionOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformSecretDefinitionKeys.list(args),
    queryFn: () => getClient().platformSecretDefinition.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * const data = await fetchPlatformSecretDefinitionsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformSecretDefinitionsQuery<
  S extends PlatformSecretDefinitionSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformSecretDefinitionFilter, PlatformSecretDefinitionOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformSecretDefinitionSelect>;
}): Promise<{
  platformSecretDefinitions: ConnectionResult<
    InferSelectResult<PlatformSecretDefinitionWithRelations, S>
  >;
}>;
export async function fetchPlatformSecretDefinitionsQuery(params: {
  selection: ListSelectionConfig<
    PlatformSecretDefinitionSelect,
    PlatformSecretDefinitionFilter,
    PlatformSecretDefinitionOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformSecretDefinitionSelect,
    PlatformSecretDefinitionFilter,
    PlatformSecretDefinitionOrderBy
  >(params.selection);
  return getClient().platformSecretDefinition.findMany(args).unwrap();
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * await prefetchPlatformSecretDefinitionsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformSecretDefinitionsQuery<
  S extends PlatformSecretDefinitionSelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformSecretDefinitionFilter, PlatformSecretDefinitionOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformSecretDefinitionSelect>;
  }
): Promise<void>;
export async function prefetchPlatformSecretDefinitionsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformSecretDefinitionSelect,
      PlatformSecretDefinitionFilter,
      PlatformSecretDefinitionOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformSecretDefinitionSelect,
    PlatformSecretDefinitionFilter,
    PlatformSecretDefinitionOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformSecretDefinitionKeys.list(args),
    queryFn: () => getClient().platformSecretDefinition.findMany(args).unwrap(),
  });
}
