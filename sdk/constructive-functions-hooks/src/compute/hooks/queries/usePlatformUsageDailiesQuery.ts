/**
 * List query hook for PlatformUsageDaily
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { QueryClient,UseQueryOptions, UseQueryResult } from '@tanstack/react-query';
import { useQuery } from '@tanstack/react-query';

import type {
  PlatformUsageDailyFilter,
  PlatformUsageDailyOrderBy,
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
} from '../../orm/input-types';
import type {
  ConnectionResult,
  HookStrictSelect,
  InferSelectResult,
} from '../../orm/select-types';
import { getClient } from '../client';
import { platformUsageDailyKeys } from '../query-keys';
import type { ListSelectionConfig } from '../selection';
import { buildListSelectionArgs } from '../selection';
export type {
  PlatformUsageDailyFilter,
  PlatformUsageDailyOrderBy,
  PlatformUsageDailySelect,
  PlatformUsageDailyWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformUsageDailiesQueryKey = platformUsageDailyKeys.list;
/**
 * Query hook for fetching PlatformUsageDaily list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformUsageDailiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformUsageDailiesQuery<
  S extends PlatformUsageDailySelect,
  TData = {
    platformUsageDailies: ConnectionResult<InferSelectResult<PlatformUsageDailyWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformUsageDailyFilter, PlatformUsageDailyOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformUsageDailySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformUsageDailies: ConnectionResult<
          InferSelectResult<PlatformUsageDailyWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformUsageDailiesQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformUsageDailySelect,
      PlatformUsageDailyFilter,
      PlatformUsageDailyOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformUsageDailySelect,
    PlatformUsageDailyFilter,
    PlatformUsageDailyOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformUsageDailyKeys.list(args),
    queryFn: () => getClient().platformUsageDaily.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch PlatformUsageDaily list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPlatformUsageDailiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformUsageDailiesQuery<S extends PlatformUsageDailySelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlatformUsageDailyFilter, PlatformUsageDailyOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlatformUsageDailySelect>;
}): Promise<{
  platformUsageDailies: ConnectionResult<InferSelectResult<PlatformUsageDailyWithRelations, S>>;
}>;
export async function fetchPlatformUsageDailiesQuery(params: {
  selection: ListSelectionConfig<
    PlatformUsageDailySelect,
    PlatformUsageDailyFilter,
    PlatformUsageDailyOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformUsageDailySelect,
    PlatformUsageDailyFilter,
    PlatformUsageDailyOrderBy
  >(params.selection);
  return getClient().platformUsageDaily.findMany(args).unwrap();
}
/**
 * Prefetch PlatformUsageDaily list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPlatformUsageDailiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformUsageDailiesQuery<S extends PlatformUsageDailySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformUsageDailyFilter, PlatformUsageDailyOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformUsageDailySelect>;
  }
): Promise<void>;
export async function prefetchPlatformUsageDailiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformUsageDailySelect,
      PlatformUsageDailyFilter,
      PlatformUsageDailyOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformUsageDailySelect,
    PlatformUsageDailyFilter,
    PlatformUsageDailyOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformUsageDailyKeys.list(args),
    queryFn: () => getClient().platformUsageDaily.findMany(args).unwrap(),
  });
}
