/**
 * Branch heads — mutable pointers into the commit chain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type { RefPatch,RefSelect, RefWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { refMutationKeys } from '../mutation-keys';
import { refKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { RefPatch,RefSelect, RefWithRelations } from '../../orm/input-types';
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRefMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', refPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRefMutation<S extends RefSelect>(
  params: {
    selection: {
      fields: S & RefSelect;
    } & HookStrictSelect<NoInfer<S>, RefSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRef: {
          ref: InferSelectResult<RefWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        databaseId: string;
        refPatch: RefPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRef: {
      ref: InferSelectResult<RefWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    databaseId: string;
    refPatch: RefPatch;
  }
>;
export function useUpdateRefMutation(
  params: {
    selection: SelectionConfig<RefSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        databaseId: string;
        refPatch: RefPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RefSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: refMutationKeys.all,
    mutationFn: ({
      id,
      databaseId,
      refPatch,
    }: {
      id: string;
      databaseId: string;
      refPatch: RefPatch;
    }) =>
      getClient()
        .ref.update({
          where: {
            id,
            databaseId,
          },
          data: refPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: refKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: refKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
