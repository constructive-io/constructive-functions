/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { useMutation, useQueryClient } from '@tanstack/react-query';

import type { CommitSelect, CommitWithRelations, CreateCommitInput } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { commitMutationKeys } from '../mutation-keys';
import { commitKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { CommitSelect, CommitWithRelations, CreateCommitInput } from '../../orm/input-types';
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCommitMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateCommitMutation<S extends CommitSelect>(
  params: {
    selection: {
      fields: S & CommitSelect;
    } & HookStrictSelect<NoInfer<S>, CommitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createCommit: {
          commit: InferSelectResult<CommitWithRelations, S>;
        };
      },
      Error,
      CreateCommitInput['commit']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createCommit: {
      commit: InferSelectResult<CommitWithRelations, S>;
    };
  },
  Error,
  CreateCommitInput['commit']
>;
export function useCreateCommitMutation(
  params: {
    selection: SelectionConfig<CommitSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateCommitInput['commit']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<CommitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: commitMutationKeys.create(),
    mutationFn: (data: CreateCommitInput['commit']) =>
      getClient()
        .commit.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: commitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
