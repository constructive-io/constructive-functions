/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import type { QueryClient,UseQueryOptions, UseQueryResult } from '@tanstack/react-query';
import { useQuery } from '@tanstack/react-query';

import type { CommitSelect, CommitWithRelations } from '../../orm/input-types';
import type { HookStrictSelect,InferSelectResult } from '../../orm/select-types';
import { getClient } from '../client';
import { commitKeys } from '../query-keys';
import type { SelectionConfig } from '../selection';
import { buildSelectionArgs } from '../selection';
export type { CommitSelect, CommitWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const commitQueryKey = commitKeys.detail;
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useCommitQuery<
  S extends CommitSelect,
  TData = {
    commit: InferSelectResult<CommitWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CommitSelect>;
  } & Omit<
    UseQueryOptions<
      {
        commit: InferSelectResult<CommitWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCommitQuery(
  params: {
    id: string;
    selection: SelectionConfig<CommitSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<CommitSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: commitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .commit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * const data = await fetchCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchCommitQuery<S extends CommitSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, CommitSelect>;
}): Promise<{
  commit: InferSelectResult<CommitWithRelations, S> | null;
}>;
export async function fetchCommitQuery(params: {
  id: string;
  selection: SelectionConfig<CommitSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<CommitSelect>(params.selection);
  return getClient()
    .commit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * await prefetchCommitQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchCommitQuery<S extends CommitSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CommitSelect>;
  }
): Promise<void>;
export async function prefetchCommitQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<CommitSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<CommitSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: commitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .commit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
