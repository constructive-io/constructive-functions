/**
 * SDK helpers — typed per-target client factories with 3-tier credential resolution
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import type { ClientConfig } from 'appstash';
import { createConfigStore } from 'appstash';

import { createClient as createApiOrmClient } from '../src/api/orm';
import { createClient as createComputeOrmClient } from '../src/compute/orm';
import { createClient as createObjectsOrmClient } from '../src/objects/orm';
const store = createConfigStore('constructive-functions');
export const getStore = () => store;
export function getClientConfig(targetName: string, contextName?: string): ClientConfig {
  return store.getClientConfig(targetName, contextName);
}
export function createApiClient(contextName?: string) {
  const config = store.getClientConfig('api', contextName);
  return createApiOrmClient({
    endpoint: config.endpoint,
    headers: config.headers,
  });
}
export function createComputeClient(contextName?: string) {
  const config = store.getClientConfig('compute', contextName);
  return createComputeOrmClient({
    endpoint: config.endpoint,
    headers: config.headers,
  });
}
export function createObjectsClient(contextName?: string) {
  const config = store.getClientConfig('objects', contextName);
  return createObjectsOrmClient({
    endpoint: config.endpoint,
    headers: config.headers,
  });
}
