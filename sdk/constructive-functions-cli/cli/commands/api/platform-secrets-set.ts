/**
 * CLI command for mutation platformSecretsSet
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';

import type { PlatformSecretsSetPayloadSelect } from '../../../orm/input-types';
import type { PlatformSecretsSetVariables } from '../../../orm/mutation';
import { getClient } from '../../executor';
import { buildSelectFromPaths,unflattenDotNotation } from '../../utils';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'platform-secrets-set - platformSecretsSet\n\nUsage: platform-secrets-set [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient('api');
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .platformSecretsSet(
        parsedAnswers as unknown as PlatformSecretsSetVariables,
        {
          select: selectFields,
        } as unknown as {
          select: PlatformSecretsSetPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: platformSecretsSet');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
