/**
 * CLI command for mutation uploadPlatformFiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';

import type { UploadPlatformFileBulkPayloadSelect } from '../../../orm/input-types';
import type { UploadPlatformFilesVariables } from '../../../orm/mutation';
import { getClient } from '../../executor';
import { buildSelectFromPaths,unflattenDotNotation } from '../../utils';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'upload-platform-files - Upload multiple files: resolves the bucket by key, creates file rows, and returns presigned PUT URLs for each.\n\nUsage: upload-platform-files [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient('api');
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .uploadPlatformFiles(
        parsedAnswers as unknown as UploadPlatformFilesVariables,
        {
          select: selectFields,
        } as unknown as {
          select: UploadPlatformFileBulkPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: uploadPlatformFiles');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
