/**
 * CLI command for mutation platformImportDefinitions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../../utils';
import type { PlatformImportDefinitionsVariables } from '../../../orm/mutation';
import type { PlatformImportDefinitionsPayloadSelect } from '../../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'platform-import-definitions - platformImportDefinitions\n\nUsage: platform-import-definitions [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .platformImportDefinitions(
        parsedAnswers as unknown as PlatformImportDefinitionsVariables,
        {
          select: selectFields,
        } as unknown as {
          select: PlatformImportDefinitionsPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: platformImportDefinitions');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
