/**
 * CLI commands for PlatformComputeLog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, extractFirst,Inquirerer } from 'inquirerer';

import type {
  CreatePlatformComputeLogInput,
  PlatformComputeLogFilter,
  PlatformComputeLogOrderBy,
  PlatformComputeLogPatch,
  PlatformComputeLogSelect,
} from '../../../orm/input-types';
import type { FindFirstArgs,FindManyArgs } from '../../../orm/select-types';
import { getClient } from '../../executor';
import type { FieldSchema } from '../../utils';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../../utils';
const fieldSchema: FieldSchema = {
  completedAt: 'string',
  id: 'uuid',
  databaseId: 'uuid',
  entityId: 'uuid',
  organizationId: 'uuid',
  entityType: 'string',
  actorId: 'uuid',
  taskIdentifier: 'string',
  jobId: 'int',
  invocationId: 'uuid',
  status: 'string',
  durationMs: 'int',
  error: 'string',
};
const usage =
  '\nplatform-compute-log <command>\n\nCommands:\n  list                  List platformComputeLog records\n  find-first            Find first matching platformComputeLog record\n  get                   Get a platformComputeLog by ID\n  create                Create a new platformComputeLog\n  update                Update an existing platformComputeLog\n  delete                Delete a platformComputeLog\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      completedAt: true,
      id: true,
      databaseId: true,
      entityId: true,
      organizationId: true,
      entityType: true,
      actorId: true,
      taskIdentifier: true,
      jobId: true,
      invocationId: true,
      status: true,
      durationMs: true,
      error: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformComputeLogSelect,
        PlatformComputeLogFilter,
        PlatformComputeLogOrderBy
      > & {
        select: PlatformComputeLogSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformComputeLog.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      completedAt: true,
      id: true,
      databaseId: true,
      entityId: true,
      organizationId: true,
      entityType: true,
      actorId: true,
      taskIdentifier: true,
      jobId: true,
      invocationId: true,
      status: true,
      durationMs: true,
      error: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformComputeLogSelect,
        PlatformComputeLogFilter,
        PlatformComputeLogOrderBy
      > & {
        select: PlatformComputeLogSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformComputeLog.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const result = await client.platformComputeLog
      .findOne({
        id: answers.id as string,
        select: {
          completedAt: true,
          id: true,
          databaseId: true,
          entityId: true,
          organizationId: true,
          entityType: true,
          actorId: true,
          taskIdentifier: true,
          jobId: true,
          invocationId: true,
          status: true,
          durationMs: true,
          error: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'organizationId',
        message: 'organizationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityType',
        message: 'entityType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'taskIdentifier',
        message: 'taskIdentifier',
        required: true,
      },
      {
        type: 'text',
        name: 'jobId',
        message: 'jobId',
        required: true,
      },
      {
        type: 'text',
        name: 'invocationId',
        message: 'invocationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: true,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformComputeLogInput['platformComputeLog'];
    const client = getClient('compute');
    const result = await client.platformComputeLog
      .create({
        data: {
          completedAt: cleanedData.completedAt,
          databaseId: cleanedData.databaseId,
          entityId: cleanedData.entityId,
          organizationId: cleanedData.organizationId,
          entityType: cleanedData.entityType,
          actorId: cleanedData.actorId,
          taskIdentifier: cleanedData.taskIdentifier,
          jobId: cleanedData.jobId,
          invocationId: cleanedData.invocationId,
          status: cleanedData.status,
          durationMs: cleanedData.durationMs,
          error: cleanedData.error,
        },
        select: {
          completedAt: true,
          id: true,
          databaseId: true,
          entityId: true,
          organizationId: true,
          entityType: true,
          actorId: true,
          taskIdentifier: true,
          jobId: true,
          invocationId: true,
          status: true,
          durationMs: true,
          error: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'organizationId',
        message: 'organizationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityType',
        message: 'entityType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'taskIdentifier',
        message: 'taskIdentifier',
        required: false,
      },
      {
        type: 'text',
        name: 'jobId',
        message: 'jobId',
        required: false,
      },
      {
        type: 'text',
        name: 'invocationId',
        message: 'invocationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: false,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformComputeLogPatch;
    const client = getClient('compute');
    const result = await client.platformComputeLog
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          completedAt: cleanedData.completedAt,
          databaseId: cleanedData.databaseId,
          entityId: cleanedData.entityId,
          organizationId: cleanedData.organizationId,
          entityType: cleanedData.entityType,
          actorId: cleanedData.actorId,
          taskIdentifier: cleanedData.taskIdentifier,
          jobId: cleanedData.jobId,
          invocationId: cleanedData.invocationId,
          status: cleanedData.status,
          durationMs: cleanedData.durationMs,
          error: cleanedData.error,
        },
        select: {
          completedAt: true,
          id: true,
          databaseId: true,
          entityId: true,
          organizationId: true,
          entityType: true,
          actorId: true,
          taskIdentifier: true,
          jobId: true,
          invocationId: true,
          status: true,
          durationMs: true,
          error: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient('compute');
    const result = await client.platformComputeLog
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
