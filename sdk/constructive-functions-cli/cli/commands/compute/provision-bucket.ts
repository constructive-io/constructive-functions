/**
 * CLI command for mutation provisionBucket
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';

import type { ProvisionBucketPayloadSelect } from '../../../orm/input-types';
import type { ProvisionBucketVariables } from '../../../orm/mutation';
import { getClient } from '../../executor';
import { buildSelectFromPaths,unflattenDotNotation } from '../../utils';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'provision-bucket - Provision an S3 bucket for a logical bucket in the database.\nReads the bucket config via RLS, then creates and configures\nthe S3 bucket with the appropriate privacy policies, CORS rules,\nand lifecycle settings.\n\nUsage: provision-bucket [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .provisionBucket(
        parsedAnswers as unknown as ProvisionBucketVariables,
        {
          select: selectFields,
        } as unknown as {
          select: ProvisionBucketPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: provisionBucket');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
