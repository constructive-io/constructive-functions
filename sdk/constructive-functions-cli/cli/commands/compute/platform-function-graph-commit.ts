/**
 * CLI commands for PlatformFunctionGraphCommit
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, extractFirst,Inquirerer } from 'inquirerer';

import type {
  CreatePlatformFunctionGraphCommitInput,
  PlatformFunctionGraphCommitFilter,
  PlatformFunctionGraphCommitOrderBy,
  PlatformFunctionGraphCommitPatch,
  PlatformFunctionGraphCommitSelect,
} from '../../../orm/input-types';
import type { FindFirstArgs,FindManyArgs } from '../../../orm/select-types';
import { getClient } from '../../executor';
import type { FieldSchema } from '../../utils';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../../utils';
const fieldSchema: FieldSchema = {
  authorId: 'uuid',
  committerId: 'uuid',
  databaseId: 'uuid',
  date: 'string',
  id: 'uuid',
  message: 'string',
  parentIds: 'uuid',
  storeId: 'uuid',
  treeId: 'uuid',
};
const usage =
  '\nplatform-function-graph-commit <command>\n\nCommands:\n  list                  List platformFunctionGraphCommit records\n  find-first            Find first matching platformFunctionGraphCommit record\n  get                   Get a platformFunctionGraphCommit by ID\n  create                Create a new platformFunctionGraphCommit\n  update                Update an existing platformFunctionGraphCommit\n  delete                Delete a platformFunctionGraphCommit\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      authorId: true,
      committerId: true,
      databaseId: true,
      date: true,
      id: true,
      message: true,
      parentIds: true,
      storeId: true,
      treeId: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformFunctionGraphCommitSelect,
        PlatformFunctionGraphCommitFilter,
        PlatformFunctionGraphCommitOrderBy
      > & {
        select: PlatformFunctionGraphCommitSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      authorId: true,
      committerId: true,
      databaseId: true,
      date: true,
      id: true,
      message: true,
      parentIds: true,
      storeId: true,
      treeId: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformFunctionGraphCommitSelect,
        PlatformFunctionGraphCommitFilter,
        PlatformFunctionGraphCommitOrderBy
      > & {
        select: PlatformFunctionGraphCommitSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit
      .findOne({
        id: answers.id as string,
        select: {
          authorId: true,
          committerId: true,
          databaseId: true,
          date: true,
          id: true,
          message: true,
          parentIds: true,
          storeId: true,
          treeId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'authorId',
        message: 'authorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'committerId',
        message: 'committerId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'date',
        message: 'date',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'message',
        message: 'message',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentIds',
        message: 'parentIds',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: true,
      },
      {
        type: 'text',
        name: 'treeId',
        message: 'treeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformFunctionGraphCommitInput['platformFunctionGraphCommit'];
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit
      .create({
        data: {
          authorId: cleanedData.authorId,
          committerId: cleanedData.committerId,
          databaseId: cleanedData.databaseId,
          date: cleanedData.date,
          message: cleanedData.message,
          parentIds: cleanedData.parentIds,
          storeId: cleanedData.storeId,
          treeId: cleanedData.treeId,
        },
        select: {
          authorId: true,
          committerId: true,
          databaseId: true,
          date: true,
          id: true,
          message: true,
          parentIds: true,
          storeId: true,
          treeId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'authorId',
        message: 'authorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'committerId',
        message: 'committerId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'date',
        message: 'date',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'message',
        message: 'message',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentIds',
        message: 'parentIds',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
      },
      {
        type: 'text',
        name: 'treeId',
        message: 'treeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformFunctionGraphCommitPatch;
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          authorId: cleanedData.authorId,
          committerId: cleanedData.committerId,
          databaseId: cleanedData.databaseId,
          date: cleanedData.date,
          message: cleanedData.message,
          parentIds: cleanedData.parentIds,
          storeId: cleanedData.storeId,
          treeId: cleanedData.treeId,
        },
        select: {
          authorId: true,
          committerId: true,
          databaseId: true,
          date: true,
          id: true,
          message: true,
          parentIds: true,
          storeId: true,
          treeId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient('compute');
    const result = await client.platformFunctionGraphCommit
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
