/**
 * CLI commands for PlatformFunctionInvocation
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, extractFirst,Inquirerer } from 'inquirerer';

import type {
  CreatePlatformFunctionInvocationInput,
  PlatformFunctionInvocationFilter,
  PlatformFunctionInvocationOrderBy,
  PlatformFunctionInvocationPatch,
  PlatformFunctionInvocationSelect,
} from '../../../orm/input-types';
import type { FindFirstArgs,FindManyArgs } from '../../../orm/select-types';
import { getClient } from '../../executor';
import type { FieldSchema } from '../../utils';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../../utils';
const fieldSchema: FieldSchema = {
  createdAt: 'string',
  actorId: 'uuid',
  completedAt: 'string',
  databaseId: 'uuid',
  durationMs: 'int',
  error: 'string',
  graphExecutionId: 'uuid',
  id: 'uuid',
  jobId: 'int',
  parentInvocationId: 'uuid',
  payload: 'json',
  result: 'json',
  startedAt: 'string',
  status: 'string',
  taskIdentifier: 'string',
};
const usage =
  '\nplatform-function-invocation <command>\n\nCommands:\n  list                  List platformFunctionInvocation records\n  find-first            Find first matching platformFunctionInvocation record\n  get                   Get a platformFunctionInvocation by ID\n  create                Create a new platformFunctionInvocation\n  update                Update an existing platformFunctionInvocation\n  delete                Delete a platformFunctionInvocation\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      actorId: true,
      completedAt: true,
      databaseId: true,
      durationMs: true,
      error: true,
      graphExecutionId: true,
      id: true,
      jobId: true,
      parentInvocationId: true,
      payload: true,
      result: true,
      startedAt: true,
      status: true,
      taskIdentifier: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformFunctionInvocationSelect,
        PlatformFunctionInvocationFilter,
        PlatformFunctionInvocationOrderBy
      > & {
        select: PlatformFunctionInvocationSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      actorId: true,
      completedAt: true,
      databaseId: true,
      durationMs: true,
      error: true,
      graphExecutionId: true,
      id: true,
      jobId: true,
      parentInvocationId: true,
      payload: true,
      result: true,
      startedAt: true,
      status: true,
      taskIdentifier: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformFunctionInvocationSelect,
        PlatformFunctionInvocationFilter,
        PlatformFunctionInvocationOrderBy
      > & {
        select: PlatformFunctionInvocationSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation
      .findOne({
        id: answers.id as string,
        select: {
          createdAt: true,
          actorId: true,
          completedAt: true,
          databaseId: true,
          durationMs: true,
          error: true,
          graphExecutionId: true,
          id: true,
          jobId: true,
          parentInvocationId: true,
          payload: true,
          result: true,
          startedAt: true,
          status: true,
          taskIdentifier: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'graphExecutionId',
        message: 'graphExecutionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'jobId',
        message: 'jobId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentInvocationId',
        message: 'parentInvocationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'payload',
        message: 'payload',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'result',
        message: 'result',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'taskIdentifier',
        message: 'taskIdentifier',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformFunctionInvocationInput['platformFunctionInvocation'];
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation
      .create({
        data: {
          actorId: cleanedData.actorId,
          completedAt: cleanedData.completedAt,
          databaseId: cleanedData.databaseId,
          durationMs: cleanedData.durationMs,
          error: cleanedData.error,
          graphExecutionId: cleanedData.graphExecutionId,
          jobId: cleanedData.jobId,
          parentInvocationId: cleanedData.parentInvocationId,
          payload: cleanedData.payload,
          result: cleanedData.result,
          startedAt: cleanedData.startedAt,
          status: cleanedData.status,
          taskIdentifier: cleanedData.taskIdentifier,
        },
        select: {
          createdAt: true,
          actorId: true,
          completedAt: true,
          databaseId: true,
          durationMs: true,
          error: true,
          graphExecutionId: true,
          id: true,
          jobId: true,
          parentInvocationId: true,
          payload: true,
          result: true,
          startedAt: true,
          status: true,
          taskIdentifier: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'graphExecutionId',
        message: 'graphExecutionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'jobId',
        message: 'jobId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentInvocationId',
        message: 'parentInvocationId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'payload',
        message: 'payload',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'result',
        message: 'result',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'taskIdentifier',
        message: 'taskIdentifier',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformFunctionInvocationPatch;
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          actorId: cleanedData.actorId,
          completedAt: cleanedData.completedAt,
          databaseId: cleanedData.databaseId,
          durationMs: cleanedData.durationMs,
          error: cleanedData.error,
          graphExecutionId: cleanedData.graphExecutionId,
          jobId: cleanedData.jobId,
          parentInvocationId: cleanedData.parentInvocationId,
          payload: cleanedData.payload,
          result: cleanedData.result,
          startedAt: cleanedData.startedAt,
          status: cleanedData.status,
          taskIdentifier: cleanedData.taskIdentifier,
        },
        select: {
          createdAt: true,
          actorId: true,
          completedAt: true,
          databaseId: true,
          durationMs: true,
          error: true,
          graphExecutionId: true,
          id: true,
          jobId: true,
          parentInvocationId: true,
          payload: true,
          result: true,
          startedAt: true,
          status: true,
          taskIdentifier: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient('compute');
    const result = await client.platformFunctionInvocation
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
