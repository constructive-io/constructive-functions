/**
 * CLI command for mutation insertNodeAtPath
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';

import type { InsertNodeAtPathPayloadSelect } from '../../../orm/input-types';
import type { InsertNodeAtPathVariables } from '../../../orm/mutation';
import { getClient } from '../../executor';
import { buildSelectFromPaths,unflattenDotNotation } from '../../utils';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'insert-node-at-path - insertNodeAtPath\n\nUsage: insert-node-at-path [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .insertNodeAtPath(
        parsedAnswers as unknown as InsertNodeAtPathVariables,
        {
          select: selectFields,
        } as unknown as {
          select: InsertNodeAtPathPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: insertNodeAtPath');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
