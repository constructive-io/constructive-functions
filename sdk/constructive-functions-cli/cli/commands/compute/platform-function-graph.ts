/**
 * CLI commands for PlatformFunctionGraph
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, extractFirst,Inquirerer } from 'inquirerer';

import type {
  CreatePlatformFunctionGraphInput,
  PlatformFunctionGraphFilter,
  PlatformFunctionGraphOrderBy,
  PlatformFunctionGraphPatch,
  PlatformFunctionGraphSelect,
} from '../../../orm/input-types';
import type { FindFirstArgs,FindManyArgs } from '../../../orm/select-types';
import { getClient } from '../../executor';
import type { FieldSchema } from '../../utils';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../../utils';
const fieldSchema: FieldSchema = {
  context: 'string',
  createdAt: 'string',
  createdBy: 'uuid',
  databaseId: 'uuid',
  definitionsCommitId: 'uuid',
  description: 'string',
  entityId: 'uuid',
  id: 'uuid',
  isValid: 'boolean',
  name: 'string',
  storeId: 'uuid',
  updatedAt: 'string',
  validationErrors: 'json',
};
const usage =
  '\nplatform-function-graph <command>\n\nCommands:\n  list                  List platformFunctionGraph records\n  find-first            Find first matching platformFunctionGraph record\n  get                   Get a platformFunctionGraph by ID\n  create                Create a new platformFunctionGraph\n  update                Update an existing platformFunctionGraph\n  delete                Delete a platformFunctionGraph\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      context: true,
      createdAt: true,
      createdBy: true,
      databaseId: true,
      definitionsCommitId: true,
      description: true,
      entityId: true,
      id: true,
      isValid: true,
      name: true,
      storeId: true,
      updatedAt: true,
      validationErrors: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformFunctionGraphSelect,
        PlatformFunctionGraphFilter,
        PlatformFunctionGraphOrderBy
      > & {
        select: PlatformFunctionGraphSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionGraph.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      context: true,
      createdAt: true,
      createdBy: true,
      databaseId: true,
      definitionsCommitId: true,
      description: true,
      entityId: true,
      id: true,
      isValid: true,
      name: true,
      storeId: true,
      updatedAt: true,
      validationErrors: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformFunctionGraphSelect,
        PlatformFunctionGraphFilter,
        PlatformFunctionGraphOrderBy
      > & {
        select: PlatformFunctionGraphSelect;
      }
    >(argv, defaultSelect);
    const client = getClient('compute');
    const result = await client.platformFunctionGraph.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient('compute');
    const result = await client.platformFunctionGraph
      .findOne({
        id: answers.id as string,
        select: {
          context: true,
          createdAt: true,
          createdBy: true,
          databaseId: true,
          definitionsCommitId: true,
          description: true,
          entityId: true,
          id: true,
          isValid: true,
          name: true,
          storeId: true,
          updatedAt: true,
          validationErrors: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'context',
        message: 'context',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdBy',
        message: 'createdBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'definitionsCommitId',
        message: 'definitionsCommitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isValid',
        message: 'isValid',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: true,
      },
      {
        type: 'json',
        name: 'validationErrors',
        message: 'validationErrors',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformFunctionGraphInput['platformFunctionGraph'];
    const client = getClient('compute');
    const result = await client.platformFunctionGraph
      .create({
        data: {
          context: cleanedData.context,
          createdBy: cleanedData.createdBy,
          databaseId: cleanedData.databaseId,
          definitionsCommitId: cleanedData.definitionsCommitId,
          description: cleanedData.description,
          entityId: cleanedData.entityId,
          isValid: cleanedData.isValid,
          name: cleanedData.name,
          storeId: cleanedData.storeId,
          validationErrors: cleanedData.validationErrors,
        },
        select: {
          context: true,
          createdAt: true,
          createdBy: true,
          databaseId: true,
          definitionsCommitId: true,
          description: true,
          entityId: true,
          id: true,
          isValid: true,
          name: true,
          storeId: true,
          updatedAt: true,
          validationErrors: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'context',
        message: 'context',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdBy',
        message: 'createdBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'definitionsCommitId',
        message: 'definitionsCommitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isValid',
        message: 'isValid',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
      },
      {
        type: 'json',
        name: 'validationErrors',
        message: 'validationErrors',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformFunctionGraphPatch;
    const client = getClient('compute');
    const result = await client.platformFunctionGraph
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          context: cleanedData.context,
          createdBy: cleanedData.createdBy,
          databaseId: cleanedData.databaseId,
          definitionsCommitId: cleanedData.definitionsCommitId,
          description: cleanedData.description,
          entityId: cleanedData.entityId,
          isValid: cleanedData.isValid,
          name: cleanedData.name,
          storeId: cleanedData.storeId,
          validationErrors: cleanedData.validationErrors,
        },
        select: {
          context: true,
          createdAt: true,
          createdBy: true,
          databaseId: true,
          definitionsCommitId: true,
          description: true,
          entityId: true,
          id: true,
          isValid: true,
          name: true,
          storeId: true,
          updatedAt: true,
          validationErrors: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient('compute');
    const result = await client.platformFunctionGraph
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
