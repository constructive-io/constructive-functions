# Use flat node_modules layout (like npm) instead of pnpm's symlinked structure.
# Required because functions use symlinked handler.ts files and tsx resolves
# modules from the real file path — pnpm's nested symlinks break resolution.
node-linker=hoisted
